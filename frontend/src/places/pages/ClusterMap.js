import React from "react";
import ReactMapboxGl from "react-mapbox-gl";
import { ReactMapboxGlCluster } from "react-mapbox-gl-cluster";

const ClusterMap = (props) => {
    return (
        <div>
            <h1>This is my Cluster MAP</h1>
        </div>
    );
};
export default ClusterMap;
